Plugin for CudaText.
Gives commands/dialog to sort lines.

To allow to sort all text, if no selection, set the option:
create config file "settings/cuda_sort.ini" (created when you close Sort-dialog by OK),
and write option:
[op]
sort_all=1


Author: Alexey T
